/**
 * return the absolute value of n
 */
int abs(int n) { return n >= 0 ? n : -n; }

/**
 * return the max value between n, m
 */
int max(int n, int m) { return n > m ? n : m; }

/**
 * return the min value between n, m
 */
int min(int n, int m) { return n < m ? n : m; }
